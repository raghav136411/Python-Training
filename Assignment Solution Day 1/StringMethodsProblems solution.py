# len() and str() practice:

#1.create a variable and assign it the string "Python"
a = "Python"

# create another variable and assign it the length of the string assigned to the variable in step 1
b = len(a)

# 3.create a variable and use string slicing and len() to assign it the length of the slice "yth" from the string assigned  to the variable from step 1
c = len(a[1:4])

# 4.create a variable and assign it the float 1.32
d = 1.32

# 5.create a variable and assign it the string "2" from the float assigned to the variable from the last problem (use the  str() string method for this)
e = str(d)
f = e[3]




# .upper() and .lower() practice:

# 1.create a variable and assign it the string "upper" changed to "UPPER" using .upper()
f = "upper"
g = f.upper()

# 2.create a variable and assign it the string "owe" from "LOWER" using string slicing and .lower()
h ="LOWER"
i = print(h[1:4].lower())